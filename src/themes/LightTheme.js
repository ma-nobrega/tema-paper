import { MD3LightTheme } from "react-native-paper";

const LightTheme = {
  ...MD3LightTheme,
  colors: {
    ...MD3LightTheme.colors,
    primary: "#3498db",
    background: "#ffffff",
    text: "#000000",
  },
};

export default LightTheme;
